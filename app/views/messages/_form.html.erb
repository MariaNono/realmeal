<%= simple_form_for([ event, message ], remote: true) do |form| %>
  <%= form.label :content, 'Write a message' %>
  <%= form.input :content, as: :text, label: false %>
  <%= form.button :submit, method: :post, class: 'btn btn-info' %>
<% end %>
