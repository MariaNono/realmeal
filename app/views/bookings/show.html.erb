<div class="container">
  <div class="row py-4">
    <div class="col-md-3"><%= cl_image_tag @booking.event.photo, width: '100%' %></div>
    <div class="col">
      <h1>Booking #<%= @booking.id %> <small><span class="badge badge-primary"><%= @booking.state %></span></small></h1>
      <ul class="list-unstyled">
        <li><strong>ISSUED ON</strong> <%= @booking.updated_at.to_date %></li>
        <li><strong>EVENT</strong> <%= @booking.event.name %></li>
        <!-- <li><strong>TOTAL</strong>€<%#='%.2f' % @booking.event.price_per_guest.to_f%></li> -->
        <li><strong>TOTAL</strong><%= humanized_money_with_symbol @booking.event.price_per_guest_cents %></li>
        </li>
      </ul>
      <!-- <p>Estimated delivery in <strong>2 business days</strong></p> -->
    </div>
  </div>
</div>
